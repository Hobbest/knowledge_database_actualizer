{
  "name": "history",
  "rules": [
    "Keep chronology, geography, actors, and primary-source attribution explicit",
    "Separate contemporary claims from later historical interpretation",
    "Retain disputed accounts and uncertainty instead of reconciling them silently"
  ]
}
